{
  "name": "pattern_editor",
  "scripts": {
    "build:wasm": "wasm-pack build",
    "build": "bun run build:wasm && bun run build.ts",
    "dev": "bun run build:wasm && bun run server.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "esbuild": "^0.19.0",
    "esbuild-plugin-wasm": "^1.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
